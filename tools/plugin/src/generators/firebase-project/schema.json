{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "firebase-project",
  "title": "Integrate Firebase Project",
  "description": "Integrate an existing Firebase project (created with firebase init) into your Nx workspace",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name for the Firebase project in the Nx workspace",
      "pattern": "^[a-zA-Z][a-zA-Z0-9-]*$",
      "x-prompt": "What name would you like to use for this project in your Nx workspace?"
    },
    "initDir": {
      "type": "string",
      "description": "Path to the directory where you ran 'firebase init'",
      "x-prompt": "What is the path to the directory where you ran 'firebase init'?"
    },
    "directory": {
      "type": "string", 
      "description": "Optional directory path for the project (defaults to project name). Creates apps/<directory>/firebase",
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-/]*[a-zA-Z0-9]$|^[a-zA-Z0-9]$",
      "x-prompt": {
        "message": "Directory path for the project (or press enter to use project name)?",
        "type": "input"
      },
      "default": ""
    }
  },
  "required": ["name", "initDir"],
  "additionalProperties": false
}
