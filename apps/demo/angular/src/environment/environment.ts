// This file will be replaced on build. Look in `project.json` for the replacement configuration.

import { Environment } from '@firemono/demo-angular-environment';
export const environment = {} as unknown as Environment;
